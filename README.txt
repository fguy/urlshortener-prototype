HTTP REST-based URL Shortner service.

-----

You can run the service with the following command:

mvn -Dport=PORT_NUMBER clean compile exec:java

Then you can access on web browser: http://localhost:PORT_NUMBER/
